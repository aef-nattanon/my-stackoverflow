# Build my stackoverflow

- Update number version at `package.json` file.
- Build project to .vsix file with this cmd.

```bash
vsce package
```

- Publish project new version with this cmd.

```bash
vsce publish
```
